apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: dummy-prod-flux-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: dummy-prod-flux.klimdos.cf
    http:
      paths:
      - backend:
          serviceName: dummy-prod-flux-svc
          servicePort: 80
        # path: /
